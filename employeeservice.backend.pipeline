pipeline {
    agent any
    
    triggers {
        pollSCM '* * * * *'
    }
    
    stages {
        stage('build') {
            steps {
                sh 'mvn clean package -DskipTests'
            }            
            
		}
	}	
}