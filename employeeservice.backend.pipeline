pipeline {
    agent any
    
    triggers {
        pollSCM '* * * * *'
    }
    
    stages {
        stage('build') {
            steps {
                sh './mvnw clean package'
            }
            
            stage('deploy') {
            sh ./mvnw spring-boot:run 
        }
    }
}