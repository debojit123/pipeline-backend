pipeline {
    agent any
    
    triggers {
        pollSCM '* * * * *'
    }
    
    stages {
        stage('build') {
            steps {
                sh './mvn clean package'
            }
            
            stage('deploy') {
            sh ./mvn spring-boot:run 
        }
    }
}